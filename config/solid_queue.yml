default: &default
  dispatchers:
    - polling_interval: 1
      batch_size: 500
      recurring_tasks:
        weekly_digest:
          class: WeeklyDigestJob
          schedule: "0 16 * * 5"

  workers:
    - queues: "*"
      threads: 3

development:
  <<: *default

production:
  <<: *default
