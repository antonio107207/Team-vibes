<% if entries.next_page %>
  <div data-infinite-scroll-target="sentinel"
       data-url="<%= trending_path(page: entries.next_page, format: :turbo_stream) %>"
       class="flex justify-center py-6">
    <svg class="w-5 h-5 text-indigo-400 animate-spin" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
      <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"/>
      <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8v8H4z"/>
    </svg>
  </div>
<% end %>
