<%= turbo_stream.replace "follow_#{@user.id}" do %>
  <%= render "shared/follow_button", user: @user %>
<% end %>
<%= turbo_stream.replace "follow_counts_#{@user.id}" do %>
  <%= render "profiles/follow_counts", user: @user %>
<% end %>
