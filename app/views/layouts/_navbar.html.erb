<nav class="bg-white dark:bg-gray-800 border-b border-gray-200 dark:border-gray-700 sticky top-0 z-10 transition-colors duration-200">
  <div class="max-w-2xl mx-auto px-4 h-14 flex items-center justify-between">
    <%= link_to root_path, class: "flex items-center gap-2 font-bold text-indigo-600 dark:text-indigo-400 text-lg" do %>
      <span>✨</span><span><%= t('nav.app_name') %></span>
    <% end %>

    <div class="flex items-center gap-2">
      <%# Locale switcher %>
      <%= button_to locale_path, params: { locale: I18n.locale == :uk ? :en : :uk },
          data: { turbo: false },
          class: "px-2 py-1 rounded-lg text-xs font-semibold text-gray-500 dark:text-gray-400 hover:bg-gray-100 dark:hover:bg-gray-700 transition cursor-pointer border border-gray-200 dark:border-gray-600" do %>
        <%= I18n.locale == :uk ? "EN" : "UA" %>
      <% end %>

      <%# Theme toggle %>
      <button data-action="click->theme#toggle"
              class="p-2 rounded-lg text-gray-500 dark:text-gray-400 hover:bg-gray-100 dark:hover:bg-gray-700 transition cursor-pointer"
              title="<%= t('nav.toggle_theme') %>"
              aria-label="<%= t('nav.toggle_theme') %>">
        <svg class="w-5 h-5 hidden dark:block" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 3v1m0 16v1m9-9h-1M4 12H3m15.364-6.364l-.707.707M6.343 17.657l-.707.707M17.657 17.657l-.707-.707M6.343 6.343l-.707-.707M12 8a4 4 0 100 8 4 4 0 000-8z"/>
        </svg>
        <svg class="w-5 h-5 block dark:hidden" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20.354 15.354A9 9 0 018.646 3.646 9.003 9.003 0 0012 21a9.003 9.003 0 008.354-5.646z"/>
        </svg>
      </button>

      <% if user_signed_in? %>
        <%= link_to new_hobby_entry_path, class: "bg-indigo-600 text-white text-sm font-medium px-3 py-1.5 rounded-lg hover:bg-indigo-700 transition" do %>
          <%= t('nav.add') %>
        <% end %>

        <div class="relative" data-controller="dropdown">
          <button data-action="click->dropdown#toggle" class="flex items-center gap-2 text-sm text-gray-700 dark:text-gray-200 hover:text-gray-900 dark:hover:text-white cursor-pointer">
            <img src="<%= current_user.avatar_or_initials %>" class="w-8 h-8 rounded-full object-cover" alt="<%= current_user.name %>">
          </button>
          <div data-dropdown-target="menu" class="hidden absolute right-0 mt-2 w-48 bg-white dark:bg-gray-800 rounded-xl shadow-lg border border-gray-100 dark:border-gray-700 py-1 text-sm">
            <%= link_to current_user.name, profile_path(current_user), class: "block px-4 py-2 text-gray-700 dark:text-gray-200 hover:bg-gray-50 dark:hover:bg-gray-700 font-medium truncate" %>
            <%= link_to t('nav.edit_profile'), edit_profile_path(current_user), class: "block px-4 py-2 text-gray-600 dark:text-gray-300 hover:bg-gray-50 dark:hover:bg-gray-700" %>
            <hr class="my-1 border-gray-100 dark:border-gray-700">
            <%= link_to favorites_path, class: "flex items-center gap-2 px-4 py-2 text-gray-600 dark:text-gray-300 hover:bg-gray-50 dark:hover:bg-gray-700" do %>
              <svg class="w-4 h-4" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" d="M17 3H7a2 2 0 00-2 2v16l7-3 7 3V5a2 2 0 00-2-2z"/>
              </svg>
              <%= t('nav.favorites') %>
            <% end %>
            <%= link_to following_path, class: "flex items-center gap-2 px-4 py-2 text-gray-600 dark:text-gray-300 hover:bg-gray-50 dark:hover:bg-gray-700" do %>
              <svg class="w-4 h-4" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" d="M17 21v-2a4 4 0 00-4-4H5a4 4 0 00-4 4v2M9 11a4 4 0 100-8 4 4 0 000 8zM23 21v-2a4 4 0 00-3-3.87M16 3.13a4 4 0 010 7.75"/>
              </svg>
              <%= t('nav.following') %>
            <% end %>
            <hr class="my-1 border-gray-100 dark:border-gray-700">
            <%= button_to t('nav.sign_out'), destroy_user_session_path, method: :delete, class: "w-full text-left px-4 py-2 text-red-600 dark:text-red-400 hover:bg-gray-50 dark:hover:bg-gray-700 cursor-pointer" %>
          </div>
        </div>
      <% end %>
    </div>
  </div>

  <% if user_signed_in? %>
    <div class="max-w-2xl mx-auto px-4 pb-2 flex gap-1 overflow-x-auto scrollbar-hide text-sm">
      <% following_active = request.path == root_path && params[:filter] == "following" %>
      <% all_active = request.path == root_path && params[:filter] != "following" %>
      <%= link_to root_path, class: "shrink-0 px-3 py-1 rounded-full #{all_active ? 'bg-indigo-100 dark:bg-indigo-900 text-indigo-700 dark:text-indigo-300 font-medium' : 'text-gray-500 dark:text-gray-400 hover:bg-gray-100 dark:hover:bg-gray-700'}" do %><%= t('nav.all') %><% end %>
      <%= link_to root_path(filter: "following"), class: "shrink-0 px-3 py-1 rounded-full #{following_active ? 'bg-indigo-100 dark:bg-indigo-900 text-indigo-700 dark:text-indigo-300 font-medium' : 'text-gray-500 dark:text-gray-400 hover:bg-gray-100 dark:hover:bg-gray-700'}" do %><%= t('nav.following_tab') %><% end %>
      <% HobbyEntry.categories.each_key do |cat| %>
        <%= link_to category_path(cat), class: "shrink-0 px-3 py-1 rounded-full #{params[:category] == cat ? 'bg-indigo-100 dark:bg-indigo-900 text-indigo-700 dark:text-indigo-300 font-medium' : 'text-gray-500 dark:text-gray-400 hover:bg-gray-100 dark:hover:bg-gray-700'}" do %>
          <%= HobbyEntry::CATEGORY_ICONS[cat] %> <%= t("categories.#{cat}") %>
        <% end %>
      <% end %>
    </div>
  <% end %>
</nav>
